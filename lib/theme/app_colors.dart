import 'package:flutter/material.dart';

// Sky blue color scheme (shiny / vibrant)
// Note: we keep the same constant names so the rest of the app updates automatically.
const kPrimaryYellow = Color(0xFF00B0FF); // Primary sky blue
const kCardYellow = Color(0xFF40C4FF); // Lighter card blue
const kCardLight = Color(0xFFE1F5FE); // Very light blue for cards/backgrounds
const kSoftYellow = Color(0xFF81D4FA); // Soft blue accents
const kMutedYellow = Color(0xFF0288D1); // Darker blue for emphasis
const kInputBackground = Color(0xFFE3F2FD); // Pale blue input background

// Keep old names for backward compatibility (pointing to sky blue palette)
const kPrimaryTeal = kPrimaryYellow;
const kCardTeal = kCardYellow;
const kSoftTeal = kSoftYellow;
const kMutedTeal = kMutedYellow;

